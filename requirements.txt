# =============================================================================
# LLM Analysis — Python dependencies
#
# Install:
#   python -m pip install -r requirements.txt
#   python -m playwright install chromium
#
# System tools (NOT installed via pip — see README):
#   - Node.js + npx          MCP contact/direction search (npx @playwright/mcp)
#   - Tesseract OCR          pytesseract fallback; need eng + chi_sim language packs
#   - Playwright Chromium    installed by command above
#
# Optional:
#   - opencv-python          OCR image preprocessing (auto-skipped if missing)
#   - Ollama                 local LLM fallback when no API key
# =============================================================================

# Core HTTP/API (New API gateway via requests; no openai SDK)
requests>=2.31.0
urllib3>=2.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# Web scraping and main-content extraction
beautifulsoup4>=4.12.0
lxml>=4.9.0
trafilatura>=1.8.0
lxml_html_clean>=0.1.0

# Progress
tqdm>=4.66.0

# PDF processing
PyMuPDF>=1.23.0
pdfplumber>=0.9.0
PyPDF2>=3.0.0

# OCR and images
pytesseract>=0.3.10
Pillow>=10.0.0

# Browser automation (dynamic pages, PDF viewers, Playwright worker)
playwright>=1.40.0

# MCP client (Playwright MCP — requires Node.js npx at runtime)
mcp>=1.9.0

# Google Sheets API
google-api-python-client>=2.117.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.0.0
google-auth>=2.17.0

# Text processing (Sheets label inflection, etc.)
inflect>=6.0.0

# -----------------------------------------------------------------------------
# Optional — OCR image preprocessing; skipped automatically if not installed
# -----------------------------------------------------------------------------
# opencv-python>=4.8.0
